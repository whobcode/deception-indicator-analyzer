# deception-indicator-analyzer

Cloudflare Worker — repo reconstructed from the live deployment and wired to auto-deploy via GitHub Actions (`.github/workflows/deploy.yml`).

- Account deploy target: `deception-indicator-analyzer`
- compatibility_date: `2024-01-23`

## Heads up
This Worker uses: **assets, secrets**. The source here is the *deployed bundle* pulled from Cloudflare. Static **assets**, **Durable Object** migration history, and cross-Worker **service bindings** may need manual finishing before a clean redeploy.

_Secrets (if any) persist on the Worker and are not stored in this repo._
